<Window x:Class="OnlyMSlideManager.Windows.MainWindow"

        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"

        DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}"

        xmlns:resx="clr-namespace:OnlyMSlideManager.Properties"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlyMSlideManager"
        xmlns:models="clr-namespace:OnlyMSlideManager.Models"
        mc:Ignorable="d"
        Title="S o u n d B o x - Slideshow Manager" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ImageCard" TargetType="{x:Type materialDesign:Card}">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Margin" Value="5" />
        </Style>

        <DataTemplate DataType="{x:Type models:SlideItem}">
            <materialDesign:Card Margin="10,10,0,0" Padding="10" Width="220" Height="215">
                <StackPanel Orientation="Vertical">
                    <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                    <Image Source="{Binding Image}" Height="100" HorizontalAlignment="Left"/>
                </StackPanel>
            </materialDesign:Card>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding SlideItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
</Window>
